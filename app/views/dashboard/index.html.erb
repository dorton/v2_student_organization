<h1><%= @city.name %></h1>

<% @days.each do |day| %>

  <%= link_to day.cohort.start_date, cohort_day_path(day.cohort.id, day.id)%>

  <%=  %>

  <% day.groups.each do |daygroup| %>
    <%= daygroup.name %>
  <% end %>

<% end %>
